<!doctype html>
<html>

<head>
  <title>VibeVerse</title>
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
  <link rel="shortcut icon" href="favicon.ico">
  <link rel="stylesheet" href="style.css">
  <link rel="stylesheet" href="profile-style.css">
  <link href="https://fonts.googleapis.com/css2?family=Lora&display=swap" rel="stylesheet">
</head>

<body>
  <div class="container">

    <div class="page-header text-center">
      <h1>Music</h1>
      <button class="btn btn-secondary"><a href="/logout">Logout</a></button>
    </div>

    <div class="row">
      <div class="topSection">

      </div>
      <!-- LOCAL INFORMATION -->
      <!-- <div class="col-sm-6">
            <div class="well">
                <h3><span class="fa fa-user"></span> Local</h3>

                <% if (user.local.email) { %>
                    <p>
                        <strong>id</strong>: <%= user._id %><br>
                        <strong>email</strong>: <%= user.local.email %><br>
                        <strong>password</strong>: <%= user.local.password %>
                    </p>

                    <a href="/unlink/local" class="btn btn-default">Unlink</a>
                <% } else { %>
                    <a href="/connect/local" class="btn btn-default">Connect Local</a>
                <% } %>

            </div>
        </div> -->
      <div class="col-sm-6">
        <h2>Add a song</h2>
        <% if (user.local.email) { %>
          <form action="/messages" method="POST">
            <input class="form-control" type="text" placeholder="Artist" name="name">
            <input class="form-control" type="text" placeholder="Song Name" name="msg">
            <button class="btn btn-secondary" type="submit">Submit</button>
          </form>
          <% } %>
      </div>
      <div class="col-sm-6">
        <h2>My List</h2>
        <ol class="messages">
          <% messages.forEach(message=> { %>
            <li class="message" data-id="<%= message.id %>">
              <span><%= message.name %></span>
              &mdash;
              <span><%= message.msg %></span>
              <span><i class="fa fa-trash" aria-hidden="true"></i></span>
            </li>
          <% }); %>
        </ol>
      </div>


    </div>

    <script src="main.js"></script>

</body>

</html>